<section class="flex-column">
  <h1>Tu Empresa</h1>
  <p>Breve descripción del negocio o propuesta de valor.</p>
  <div class="buttons flex-row">
    <a class="boton button1" href="#contacto">Contáctanos</a>
    <a class="boton2 button2" href="#servicios">Saber Más</a>
  </div>
</section>

<style>
  section {
    text-align: center;
    background-color: var(--clr-dark);
    background-image: var(--dot-grid);
    background-size: var(--dot-grid-size);
    border-bottom: solid 1px var(--border-soft);
    min-height: 340px;
    padding: 64px 20px;
    gap: 20px;
  }

  h1 {
    font-family: var(--font-display);
    font-size: 2.9rem;
    line-height: 1.02;
    font-weight: 800;
    letter-spacing: -0.03em;
    color: var(--clr-white);
    max-width: 12ch;
  }

  p {
    color: var(--clr-dark2);
    font-size: 1rem;
    max-width: 46ch;
  }

  .buttons {
    gap: 10px;
    margin-top: 10px;
  }

  @media (min-width: 750px) {
    h1 {
      font-size: 5.2rem;
    }
  }
</style>
